# This file was generated by generate-sync-all script. Don't edit it directly.

name: Sync Files & Secrets
'on':
  push:
    branches:
      - master
      - main
jobs:
  sync-npm-packages:
    runs-on: ubuntu-latest
    steps:
      - uses: google/secrets-sync-action@master
        with:
          secrets: ^NPM_TOKEN$
          repositories_list_regex: false
          repositories: |-
            zardoy/vk-params
            zardoy/read-browser-tabs
            zardoy/craco-electron
            zardoy/world-to-world
          github_token: ${{ secrets.SYNC_GITHUB_TOKEN }}
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
  sync-websites:
    runs-on: ubuntu-latest
    steps:
      - uses: adrianjost/files-sync-action@master
        with:
          GITHUB_TOKEN: ${{ secrets.SYNC_GITHUB_TOKEN }}
          SRC_ROOT: sync/websites
          COMMIT_MESSAGE: 'sync-files: update files from "%SRC_REPO%"'
          FILE_PATTERNS: ^\.github/workflows/gh\x2dpages\.yml$
          TARGET_REPOS: |-
            zardoy/zardoy-site
            zardoy/tell-tale-about-borow
            zardoy/ggworlds.ru
