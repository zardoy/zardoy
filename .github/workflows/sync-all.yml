# This file was generated by generate-sync-all script. Don't edit it directly.

name: Sync Files & Secrets
'on':
  push:
    branches:
      - master
      - main
jobs:
  sync-npm-packages:
    runs-on: ubuntu-latest
    steps:
      - uses: google/secrets-sync-action@master
        with:
          dry_run: true
          secrets: ^NPM_TOKEN$
          repositories: |-
            ^zardoy/vk\x2dparams$
            ^zardoy/read\x2dbrowser\x2dtabs$
            ^zardoy/craco\x2delectron$
            ^zardoy/world\x2dto\x2dworld$
          github_token: ${{ secrets.SYNC_GITHUB_TOKEN }}
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
  sync-websites:
    runs-on: ubuntu-latest
    steps:
      - uses: adrianjost/files-sync-action@master
        with:
          DRY_RUN: true
          GITHUB_TOKEN: ${{ secrets.SYNC_GITHUB_TOKEN }}
          SRC_ROOT: sync/websites
          FILE_PATTERNS: .*
          TARGET_REPOS: |-
            zardoy/zardoy-site
            zardoy/tell-tale-about-borow
            zardoy/ggworlds.ru
